import type { Metadata, Viewport } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "Poker Settler - Calculate Poker Game Settlements",
  description: "A modern web app for calculating poker game settlements. Track buy-ins, chips, and calculate who pays whom after the game.",
  keywords: "poker, settlement, calculator, game, chips, buy-in",
  authors: [{ name: "Poker Settler" }],
};

export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} font-sans antialiased`}>
        {children}
      </body>
    </html>
  );
}
